# Playground

Experiments with coding and AI.

## Omio training roadmap (static)

Published from the **`docs/`** folder via GitHub Pages:

- After Pages is enabled (branch `main`, folder `/docs`), the site is at  
  `https://stefanomonterosso-hash.github.io/playground/`  
  (redirects to `life-at-omio-roadmap.html`).

Source files: `docs/life-at-omio-roadmap.html`, `docs/assets/omio-logo.png`.
